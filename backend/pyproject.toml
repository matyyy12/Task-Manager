[project]
name = "TaskManager"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = [
    "django>=5.1",
    "django-core-headers",
    "djangorestframework>=3.17.1",
    "psycopg[binary]>=3.2",
    "python-decouple>=3.8",
]

[dependency-groups]
dev = [
    "django-stubs>=5.1",
    "mypy>=1.11",
]
